# Intentionally empty — overrides the default nginx welcome page config.
# Without this, nginx serves a 200 on port 80 for Host:localhost instead
# of redirecting to HTTPS via our mail.conf server block.
